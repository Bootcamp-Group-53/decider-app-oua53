import 'package:flutter/material.dart';

class ActionScreen extends StatelessWidget {
  final Widget widget;
  const ActionScreen({super.key, required this.widget});

  @override
  Widget build(BuildContext context) {
    return widget;
  }
}
